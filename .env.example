# Public — shipped to client bundle
PUBLIC_CITADEL_API_URL=http://localhost:8080

# Private — server-only. Basic-auth creds for Grafana alert-rules API.
# Must match Grafana admin user/password (GF_SECURITY_ADMIN_*).
GRAFANA_USER=admin
GRAFANA_PASSWORD=
